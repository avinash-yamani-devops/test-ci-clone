version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: echo "This is the build job"

  test:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: echo "This is the test job"

workflows:
  version: 2
  build_workflow:
    jobs:
      - build
      - test
